# Normal Cowork/git-clone usage does not require a .env file.
# Hare does not load this file. Developers who need an explicit local override
# must set OMH_M365_CLIENT_ID and OMH_M365_TENANT_ID in the process environment.
